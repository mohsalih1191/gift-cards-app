{
  "name": "gift-cards-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "project/frontend",
    "project/backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=project/frontend && npm run build --workspace=project/backend",
    "start": "npm start --workspace=project/backend",
    "dev": "npm run dev --workspace=project/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
